# 每日索引：运行与安装

首版采用「每日索引」视觉方向，运行在 Windows Electron 桌面端。

- 开发运行：在本目录执行 `npm.cmd install`，再执行 `npm.cmd start`。
- 自动测试：执行 `npm.cmd test`。
- Windows 安装包：`dist/每日索引 Setup 0.1.0.exe`。
- 安装后可通过托盘菜单或 `Ctrl + Alt + Space` 显示 / 隐藏挂件；应用不会开机自启。
